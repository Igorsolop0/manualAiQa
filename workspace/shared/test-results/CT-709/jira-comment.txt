Tested on the QA environment. OAuth login refactor to use ExternalId.

✅ Tested:

**Google Login Flow:**
- Existing Google user (post-migration) — works as expected, data.externalId populated
- Pre-migration Google user — backfill works correctly, data.externalId populated on first login
- New Google registration — data.externalId = email (lowercase)
- Email change scenario — login still works after email changes (ExternalId stable)
- Case-insensitive lookup — USER@EXAMPLE.COM = user@example.com

**Telegram Login Flow:**
- Existing Telegram user (post-migration) — works as expected, data.externalId = "TG{id}"
- Pre-migration Telegram user — backfill works correctly
- New Telegram registration — data.externalId = "TG{telegramUserId}"

**Uniqueness:**
- Same Google email across different partners — works (creates separate accounts)
- Duplicate Google link in same partner — blocked correctly
- Same Telegram ID across different partners — works (creates separate accounts)
- Duplicate Telegram link in same partner — blocked correctly

**Migration Verification:**
- All existing Google users have data.externalId populated (verified via API responses)
- All existing Telegram users have data.externalId populated (verified via API responses)
- No DB access used — validated through API responses only

**Regression:**
- Email/password login — works as expected
- OTP login — works as expected
- Token refresh — works as expected
- Session persistence — works as expected

📊 Results: 20/20 tests passed

📁 Evidence saved to: ~/.openclaw/workspace/shared/test-results/CT-709/
