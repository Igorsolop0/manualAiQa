# CT-548: Google Linking & Unlinking — Test Execution Task

**Ticket:** CT-548 — Frontend Task  
**Status:** Testing  
**Priority:** Normal  
**Created:** 105 days ago  
**Labels:** PostMVP_CT18, PostMVP_CT20

---

## 1. Context

### What Changed
- Client Profile отримав можливість прив'язки Google акаунту
- Frontend відправляє auth token при ініціації social linking/unlinking
- Endpoint: `/GoogleAccount/GetAuthUrl` з параметром `linkToken`

### Dependencies
- **Blocker:** CRYPTO-445 (Development) — статус невідомий
- **Blocker:** PR-2756 (Closed)
- **Depends on:** CT-751 (Done) — Backend task

### Known Issue (23 days ago)
- QA env не повертає `linkedRegistrationSources` в player object
- Потрібно перевірити чи виправлено

### Test Pages
- **QA:** https://minebit-casino.qa.sofon.one/test-social-linking
- **PROD:** https://minebit-casino.prod.sofon.one/test-social-linking

---

## 2. Test Strategy

### Mock Strategy (CRITICAL — No Real Google Accounts!)

**DO NOT use real Google accounts for testing!**

Use one of these approaches:

#### Option A: Backend Mock (Preferred)
1. Cipher створює test player через API
2. Cipher готує mock linked state через backend API
3. Clawver тестує UI з mock data

#### Option B: Playwright Network Mocking
```typescript
// Mock Google OAuth endpoints
await page.route('**/accounts.google.com/o/oauth2/v2/auth**', route => {
  route.fulfill({
    status: 302,
    headers: { 'Location': '/callback/google?code=mock-code' }
  });
});

await page.route('**/oauth2/v4/token', route => {
  route.fulfill({
    status: 200,
    body: JSON.stringify({
      access_token: 'mock-token',
      id_token: 'mock-id-token'
    })
  });
});
```

#### Option C: Google OAuth Playground + Refresh Token (Fallback)
- Create dedicated test Google account: `minebit.qa.test@gmail.com`
- Use OAuth Playground to get refresh token ONCE
- Store credentials in `.env` (NOT in Git!)
- Auto-refresh token in tests

---

## 3. Test Scenarios

### 3.1 Pre-requisites Check

| # | Scenario | Evidence | Owner |
|---|----------|----------|-------|
| 1 | Перевірити статус CRYPTO-445 blocker | Jira check | Nexus |
| 2 | Перевірити чи повертається `linkedRegistrationSources` в QA | API response | Cipher |
| 3 | Підготувати тестовий гравець без linked Google | Test account | Cipher |

### 3.2 Happy Path — Linking

| # | Scenario | Evidence | Owner |
|---|----------|----------|-------|
| 4 | Відкрити Client Profile, перевірити наявність "Link Google" кнопки | Screenshot | Clawver |
| 5 | Клікнути "Link Google" → перевірити redirect до Google OAuth (або mock) | Network log + Screenshot | Clawver |
| 6 | Завершити OAuth flow (mock) → перевірити redirect назад | Screenshot + URL | Clawver |
| 7 | Перевірити success state в профілі (Google linked) | Screenshot | Clawver |
| 8 | Перевірити `linkedRegistrationSources` в player object | API response | Cipher |

### 3.3 Happy Path — Unlinking

| # | Scenario | Evidence | Owner |
|---|----------|----------|-------|
| 9 | Відкрити профіль з linked Google → клікнути "Unlink" | Screenshot | Clawver |
| 10 | Підтвердити unlink → перевірити success state | Screenshot | Clawver |
| 11 | Перевірити що Google більше не linked в player object | API response | Cipher |

### 3.4 Error Cases

| # | Scenario | Evidence | Owner |
|---|----------|----------|-------|
| 12 | Link без auth token (logout session) → error handling | Screenshot + console | Clawver |
| 13 | Cancel Google OAuth → повернення до профілю без помилок | Screenshot | Clawver |
| 14 | Link з вже linked Google акаунтом → error message | Screenshot | Clawver |
| 15 | Invalid / expired token → error handling | Console + network | Clawver |

### 3.5 Edge Cases

| # | Scenario | Evidence | Owner |
|---|----------|----------|-------|
| 16 | Multiple link/unlink cycles → стабільна поведінка | Test sequence | Clawver |
| 17 | Page refresh під час OAuth flow → коректна обробка | Screenshot | Clawver |
| 18 | Network error під час link/unlink → error handling | Console | Clawver |

### 3.6 Backend Validation

| # | Scenario | Evidence | Owner |
|---|----------|----------|-------|
| 19 | `/GoogleAccount/GetAuthUrl` з валідним `linkToken` → 200 + auth URL | API response | Cipher |
| 20 | `/GoogleAccount/GetAuthUrl` без `linkToken` → error response | API response | Cipher |
| 21 | Player object містить `linkedRegistrationSources` після linking | API response | Cipher |

---

## 4. Execution Instructions

### Step 1: Pre-requisites (Cipher)
```bash
# Перевірити blocker статус
python3 scripts/jira_fetch.py CRYPTO-445

# Перевірити linkedRegistrationSources в QA
curl -X GET "https://minebit-casino.qa.sofon.one/api/player/{test-player-id}" \
  -H "Authorization: Bearer {token}"

# Створити тестового гравця без linked Google
python3 scripts/07_test_data_orchestrator.py --scenario player_with_balance --env qa
```

### Step 2: UI Testing (Clawver)
```bash
# Запуск browser tests на QA
URL: https://minebit-casino.qa.sofon.one/test-social-linking
Auth: workspace/shared/credentials/CT-548-credentials.json (if needed)
Output: workspace/shared/test-results/CT-548/
```

### Step 3: Evidence Collection
- Screenshots for each UI state
- Network logs for OAuth flow
- Console logs for errors
- API responses for validation

---

## 5. Expected Results

### Success Criteria
1. ✅ "Link Google" button visible in Client Profile
2. ✅ OAuth flow completes (mocked or real)
3. ✅ Success state shows "Google linked"
4. ✅ `linkedRegistrationSources` contains "google" after linking
5. ✅ Unlink removes Google from linked sources
6. ✅ Error cases handled gracefully with user-friendly messages

### Failure Indicators
- ❌ "Link Google" button missing
- ❌ OAuth flow stuck or errors
- ❌ `linkedRegistrationSources` not returned in player object
- ❌ Unlink doesn't update player state
- ❌ Errors shown as raw JSON or stack traces

---

## 6. Output Requirements

### Folder Structure
```
workspace/shared/test-results/CT-548/
├── screenshots/
│   ├── 01-link-button.png
│   ├── 02-oauth-redirect.png
│   ├── 03-success-state.png
│   ├── 04-unlink-confirm.png
│   └── 05-unlinked-state.png
├── network-logs/
│   ├── oauth-flow.json
│   └── api-calls.json
├── console-logs/
│   └── errors.log
└── CT-548-result.md
```

### Result File Format
```markdown
# CT-548 Test Results

**Date:** YYYY-MM-DD
**Environment:** QA
**Tester:** Clawver

## Summary
- Total scenarios: 21
- Passed: X
- Failed: Y
- Blocked: Z

## Detailed Results

### 3.1 Pre-requisites Check
| # | Scenario | Status | Evidence | Notes |
|---|----------|--------|----------|-------|
| 1 | CRYPTO-445 status | ✅/❌ | link | ... |
| 2 | linkedRegistrationSources check | ✅/❌ | screenshot | ... |
| 3 | Test player created | ✅/❌ | player-id | ... |

### 3.2 Happy Path — Linking
[... detailed results ...]

## Issues Found
1. [Issue description with evidence]

## Recommendations
1. [Recommendation for development team]
```

---

## 7. Security Notes

**CRITICAL:**
- ❌ DO NOT use real Google accounts
- ❌ DO NOT commit Google credentials to Git
- ✅ Use mock data or dedicated test accounts
- ✅ Store test credentials in `.env` files only
- ✅ Use separate test Google account if needed

---

## 8. References

- **Figma:** https://www.figma.com/design/qkzG2IGMeL6JEpJSmIEQxE/Minebit--Workfile-?node-id=271-56986
- **MR:** https://git.dolore.cc/portal/portal/-/merge_requests/111
- **QA Test Page:** https://minebit-casino.qa.sofon.one/test-social-linking
- **PROD Test Page:** https://minebit-casino.prod.sofon.one/test-social-linking

---

**Created:** 2026-03-18  
**Author:** Nexus  
**Executor:** Clawver (UI) + Cipher (Backend)

<!-- PHASE2_DISPATCH_BLOCK_START -->
## Phase 2 Pilot Dispatch Hooks (Auto-generated)

- **run_id:** `CT-548-20260318-01`
- **agent:** `qa-agent`
- **mode:** `dual-write` (legacy + run mirror)

After execution, run mirror sync:
```bash
python3 /Users/ihorsolopii/.openclaw/scripts/run_manager.py sync-legacy --ticket CT-548
```

Fail-fast runtime guard (required before normal emit-result in Stagehand ONLY tasks):
```bash
python3 /Users/ihorsolopii/.openclaw/scripts/run_manager.py stagehand-guard --ticket CT-548 --phase post --agent qa-agent --on-violation blocked --next-owner nexus --emit-result --write-results-stub
```

If auth/session was used in this run, register session-record (reference-only handoff):
```bash
python3 /Users/ihorsolopii/.openclaw/scripts/run_manager.py register-session --ticket CT-548 --project minebit --subject-type player --owner qa-agent --storage-state-ref workspace/shared/test-auth/prod-player-auth.json --token-ref workspace/shared/test-auth/token.txt --status active --refresh-strategy ui_login
```

Emit result packet for Nexus review:
```bash
python3 /Users/ihorsolopii/.openclaw/scripts/run_manager.py emit-result --ticket CT-548 --agent qa-agent --status completed --confidence medium --next-owner nexus --evidence-ref workspace/shared/test-results/CT-548/results.json
```

Emit learning candidate (if run produced reusable insight):
```bash
python3 /Users/ihorsolopii/.openclaw/scripts/run_manager.py emit-learning --ticket CT-548 --owner qa-agent --status completed --observed "<observed>" --impact "<impact>" --applies-to "<applies-to>" --promote-to run-only --evidence-ref workspace/shared/test-results/CT-548/results.json
```

Nexus pre-summary gate (wait for stable results + validate contracts):
```bash
python3 /Users/ihorsolopii/.openclaw/scripts/run_manager.py pre-summary-gate --ticket CT-548
```
<!-- PHASE2_DISPATCH_BLOCK_END -->
